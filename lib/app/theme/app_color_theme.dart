import 'package:flutter/material.dart';

ColorScheme appColorTheme(BuildContext context) =>
    Theme.of(context).colorScheme;

    /// get the color scheme from the context
    /// and return the color scheme
  
